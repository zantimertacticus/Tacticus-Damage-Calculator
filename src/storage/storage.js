import { STORAGE_KEYS } from "../config/constants.js";export function getPresets(){const raw=localStorage.getItem(STORAGE_KEYS.PRESETS);return raw?JSON.parse(raw):{};}export function savePreset(name,state){const p=getPresets();p[name]=state;localStorage.setItem(STORAGE_KEYS.PRESETS,JSON.stringify(p));}export function loadPreset(name){return getPresets()[name]??null;}export function deletePreset(name){const p=getPresets();delete p[name];localStorage.setItem(STORAGE_KEYS.PRESETS,JSON.stringify(p));}export function clearAllPresets(){localStorage.removeItem(STORAGE_KEYS.PRESETS);}export function downloadJson(filename,data){const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url);}